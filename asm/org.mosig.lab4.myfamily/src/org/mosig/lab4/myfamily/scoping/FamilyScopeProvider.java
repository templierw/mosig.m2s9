/*
 * generated by Xtext 2.25.0
 */
package org.mosig.lab4.myfamily.scoping;

import java.util.List;

import org.eclipse.emf.ecore.EObject;
import org.eclipse.emf.ecore.EReference;
import org.eclipse.xtext.EcoreUtil2;
import org.eclipse.xtext.scoping.IScope;
import org.eclipse.xtext.scoping.Scopes;
import org.mosig.lab4.myfamily.family.Family;
import org.mosig.lab4.myfamily.family.FamilyPackage;
import org.mosig.lab4.myfamily.family.Member;


/**
 * This class contains custom scoping description.
 * 
 * See https://www.eclipse.org/Xtext/documentation/303_runtime_concepts.html#scoping
 * on how and when to use it.
 */
public class FamilyScopeProvider extends AbstractFamilyScopeProvider {
	@Override
	public IScope getScope(EObject context, EReference reference) {
	    // We want to define the Scope for the Element's superElement cross-reference
	    if (context instanceof Family
	            && (reference == FamilyPackage.Literals.FAMILY__M || reference == FamilyPackage.Literals.FAMILY__F)) {
	        // Collect a list of candidates by going through the model
	        // EcoreUtil2 provides useful functionality to do that
	        // For example searching for all elements within the root Object's tree
	        EObject rootElement = EcoreUtil2.getRootContainer(context);
	        List<Member> candidates = EcoreUtil2.getAllContentsOfType(rootElement, Member.class);
	        
	        // Create IEObjectDescriptions and puts them into an IScope instance
	        return Scopes.scopeFor(candidates);
	    }
	    return super.getScope(context, reference);
	}
}
